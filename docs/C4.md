### C4: Context & Containers

Контекст:
```mermaid
flowchart LR
  user[(User)] --> UI[Web UI]
  UI --> Node[Node.js API]
  Node --> DB[(MySQL)]
  Node ..> Kaspi[[Kaspi API]]
  DotNet[.NET KaspiApi] ..> Kaspi
  DotNet --> DB
```

Контейнеры:
```mermaid
flowchart LR
  subgraph Node service
    routes[Express routes: auth,user,product,order,invoice,storage,cell,entity,settings]
    prisma[Prisma ORM]
  end
  subgraph KaspiApi (.NET)
    hosted[TimedHostedService]
    services[Order/Product/Storage Sync]
  end
  routes --> prisma --> DB[(MySQL)]
  services --> DB
  services ..> KaspiAPI[[Kaspi API]]
```


